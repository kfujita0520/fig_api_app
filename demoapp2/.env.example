# Server-only Figment API key (Vercel: Secret). Never use VITE_ prefix.
FIGMENT_API_KEY=your-figment-api-key

# Optional CORS allowlist (same-origin deploy usually does not need this).
# ALLOWED_ORIGINS=http://localhost:5175,https://your-app.vercel.app

# Optional local API port (default 3000)
# PORT=3000

# Frontend (Vite) — same-origin BFF by default
# VITE_FIGMENT_API_BASE=/api/figment
VITE_SOLANA_CLUSTER=devnet

# Optional RPC with full history for Activity
# VITE_SOLANA_RPC_URL=https://your-solana-rpc.example

# Optional validator vote account
# VITE_FIGMENT_VOTE_ACCOUNT=your-vote-account-pubkey
